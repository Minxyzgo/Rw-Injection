package android.net.http;

import org.apache.http.HttpRequest;
import org.apache.http.HttpRequestInterceptor;
import org.apache.http.protocol.HttpContext;

class a implements HttpRequestInterceptor {
    final AndroidHttpClient a = null;

    private a(AndroidHttpClient p0) {
        super();
    }

    a(AndroidHttpClient p0, AndroidHttpClient.AndroidHttpClient44 p1) {
        super();
    }

    @Override
    public void process(HttpRequest p0, HttpContext p1) {
    }
}
